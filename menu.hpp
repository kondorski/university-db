#pragma once

#include <iostream>
#include <vector>

#include "Student.hpp"

void menuInput (const int);
void displayMenu();
void printDatabase (std::vector<Student> &);
void menu_choice(const int input);